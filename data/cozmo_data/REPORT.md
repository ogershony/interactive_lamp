# Cozmo retargeting dataset — preprocessing report

Generated by `data_preprocessing_pipeline/run_all.py`. Regenerate with:

```
/home/oren/miniconda3/bin/python data_preprocessing_pipeline/run_all.py
```

Inputs (never modified): `raw/assets/` (pycozmo 0.8.0 resource download, Cozmo app 3.4.0 OBB) and `raw/cozmo_animation_labels.csv` (crowd emotion annotations of clip videos; `embed_code` = YouTube video id).

Outputs: `animations/json/<clip_name>.json` (keyframe ground truth, one clip per file), `labels.csv` (one aggregated annotation row per clip), `animations/npz/<clip_name>.npz` (derived 33 ms channel arrays). **Invariant: labels.csv `clip_name`, animations/json/ stems and animations/npz/ stems are identical sets — a strict bijection, enforced in stage 3 and asserted again at the end of the run.**

## Stage 1 — extract clips (s1_extract.py)

Decoded the raw FlatBuffers animation containers with `pycozmo.anim_encoder.AnimClips.from_fb_file` and wrote **one JSON file per clip**, named by the clip's embedded `Name` field.

*Why:* a `.bin` container holds up to 49 clips and is named after only one of them (14% of clip names match their container stem), so labels can only reference clips by exact file name if each clip gets its own file. `source_bin` inside each JSON preserves provenance.

- containers decoded: 289
- clips written: 993 (names verified unique)

## Stage 2 — clean and aggregate labels (s2_labels.py)

1. **Repaired 16 split rows.** `description` contains literal unescaped newlines, so a naive CSV read sees 1886 rows instead of the true 1870 and silently loses all 16 emotion values on the affected annotations. Integrity check: after stitching, the file's index column is exactly sequential 0..n-1 (hard assertion).
2. **Canonicalized clip names.** ~400 file_names are mangled (underscores as spaces, the minus in `head_angle_-20` dropped leaving a double space, `.avi` missing). `canonical_name()` undoes this; without it ~200 clips would fail to match their labels.
3. **Dropped 29 annotations with zero emotions ticked** (no label signal; their descriptions are also garbled) **and 30 with more than 7 of 16 ticked** (one rater ticked 15 — careless clicking, not perception).
4. **Cleaned descriptions**: newlines and repeated whitespace collapsed; fully-uppercase text lowercased (262 rows), otherwise left verbatim.
5. **Grouped by clip** -> one row per clip: emotion columns are the FRACTION of that clip's annotators who ticked the emotion (inter-annotator agreement is low — ~6% of multi-annotated clips agree exactly — so fractions retain signal a majority vote destroys); descriptions joined with ' | '; `youtube_url` built from `embed_code` for smoke-checking against the source videos.

- annotations: 1870 -> 1811 kept
- clips (rows in labels.csv): 930
- annotators per clip: 1×54, 2×871, 3×5

## Stage 3 — enforce label<->clip bijection (s3_bijection.py)

Rule: the dataset keeps only pairs. Annotations without an animation are useless for retargeting; animations without an annotation are unusable for emotion-conditioned work. Names must match EXACTLY (labels.csv `clip_name` == animations/json/ file stem).

- label rows dropped (no clip file): 4
  - `ANIMATION_TEST` — a filmed test animation never shipped in the game assets
  - `anim_qa_firmwaremessaging_01` — a filmed test animation never shipped in the game assets
  - `anim_triple_backup` — a filmed test animation never shipped in the game assets
  - `soundTestAnim` — a filmed test animation never shipped in the game assets
- clip files deleted (no label row): 67 (mostly `anim_codelab_*` sound-effect animals never filmed for annotation):
  - `anim_codelab_cow_01`, `anim_codelab_lion_01`, `anim_codelab_lion_01_head_angle_-20`, `anim_codelab_lion_01_head_angle_20`, `anim_codelab_lion_01_head_angle_40`, `anim_codelab_rattle_snake_01`, `anim_codelab_rooster_01`, `anim_codelab_rooster_01_head_angle_-20`, `anim_codelab_tasmanian_devil_01`, `anim_codelab_tiger_01`, `anim_cozmosays_badword_01`, `anim_cozmosays_badword_02`, `anim_cozmosays_getin_long_01`, `anim_cozmosays_getin_short_01`, `anim_cozmosays_getout_long_01`, `anim_cozmosays_getout_medium_01`, `anim_cozmosays_getout_short_01_head_angle_20`, `anim_energy_idlel2_01`, `anim_energy_idlel2_02`, `anim_energy_idlel2_03`, `anim_explorer_driving01_end_01`, `anim_explorer_driving01_loop_01`, `anim_explorer_driving01_loop_02`, `anim_explorer_driving01_loop_03`, `anim_explorer_driving01_start_02`, `anim_explorer_getout_01`, `anim_explorer_idle_01`, `anim_explorer_idle_03`, `anim_freeplay_reacttoface_identified_03`, `anim_freeplay_reacttoface_sayname_01`, `anim_freeplay_reacttoface_sayname_03`, `anim_gamesetup_getin_01`, `anim_keepalive_eyesonly_loop_01`, `anim_launch_firsttimewakeup_helloplayer`, `anim_launch_reacttocube`, `anim_launch_search`, `anim_lookinplaceforfaces_keepalive_long`, `anim_lookinplaceforfaces_keepalive_medium`, `anim_lookinplaceforfaces_keepalive_short`, `anim_meetcozmo_getin`, `anim_meetcozmo_lookface_getin`, `anim_meetcozmo_lookface_getout`, `anim_meetcozmo_sayname_01`, `anim_meetcozmo_sayname_02`, `anim_petdetection_cat_02`, `anim_petdetection_dog_02`, `anim_petdetection_misc_01`, `anim_petdetection_shortreaction_02`, `anim_pyramid_lookinplaceforfaces_long`, `anim_pyramid_lookinplaceforfaces_short`, `anim_pyramid_reacttocube_happy_high_01`, `anim_pyramid_reacttocube_happy_low_01`, `anim_pyramid_rt2blocks_01`, `anim_pyramid_thankyou_01`, `anim_pyramid_thankyou_01_head_angle_-20`, `anim_pyramid_thankyou_01_head_angle_40`, `anim_reacttoblock_ask_01`, `anim_reacttoblock_react_short_01`, `anim_reacttoblock_react_short_02`, `anim_reacttoface_unidentified_01`, `anim_reacttoface_unidentified_02`, `anim_reacttoface_unidentified_03`, `anim_repair_severe_fix_head_up_01`, `anim_speedtap_fakeout_01`, `anim_speedtap_wait_medium`, `anim_vc_reaction_alreadyhere_01`, `anim_vc_reaction_yesfaceheardyou_01`

- **final: 926 label rows == 926 clip files, verified identical name sets** (hard assertion; the pipeline aborts on any mismatch)

## Stage 4 — resample to NPZ channel arrays (s4_npz.py)

Sampled every clip on its native 33 ms authoring grid (pycozmo FRAME_RATE = 30; 96% of keyframe triggers are exact multiples of 33 ms) with pycozmo playback semantics: head/lift ramp linearly over each keyframe's duration then hold; procedural faces step at trigger (no duration in the data); body motion runs at constant speed for its duration then stops; LEDs light for their duration then turn off.

**Loss analysis** — the conversion is lossless for on-grid keyframes (a piecewise-linear signal sampled at its own breakpoints); the caveats are:
1. off-grid keyframes (~4%) incur <33 ms timing rounding,
2. `x_mm/y_mm/yaw_rad` are DERIVED via differential-drive integration (45 mm track, wheel speeds clamped to the 200 mm/s firmware limit; STRAIGHT speed = mm/s, TURN_IN_PLACE speed = wheel mm/s, arc: v = speed*radius, omega = speed) — the raw unclamped `body_speed_raw`/`body_radius_mm`/`body_mode` are stored so any other interpretation can be re-derived,
3. rare keyframe kinds are not carried: EventKeyFrame (36 total), FaceAnimationKeyFrame (2 total), RecordHeadingKeyFrame (1 total), TurnToRecordedHeadingKeyFrame (1 total) — affecting 39 clips; audio events are kept as sparse arrays (times/ids/volume), the sound content itself stays in raw/assets.

NaN in a float channel means the clip never animates that channel (the data defines no rest pose); body/LED channels use zero because stopped/off is their true neutral. `animations/json/*.json` remains the keyframe ground truth — NPZ is a derived view and can be regenerated with different choices at any time.

- clips converted: 926
- total frames: 91008 (3003.3 s at 30.3 Hz)

### Corpus statistics (npz)

| stat | value |
|---|---|
| clips | 926 |
| total duration | 2972.5 s (49.5 min) |
| clip duration min / median / mean / max | 0.00 / 2.48 / 3.21 / 29.70 s |
| frames (33 ms) | 91008 |
| head angle: coverage, range | 823/926 clips (89%), -25 … 45 deg |
| lift height: coverage, range | 660/926 clips (71%), 0 … 92 mm |
| body: coverage, v, omega | 685/926 clips (74%), -200 … 200 mm/s, -8.9 … 8.9 rad/s (post-clamp) |
| integrated displacement per clip | median 18 mm, max 1101 mm |
| face coverage | 918/926 clips (99%) |
| LEDs coverage | 87/926 clips (9%) |
| audio coverage | 879/926 clips (95%), 3881 events total |
| eye openness range (derived) | 0.00 … 1.71 |

## Final state

- **926 clips**, each with exactly one labels.csv row, one animations/json/*.json file and one animations/npz/*.npz file, all under the same exact name
- annotations aggregated: 1811
- resampled frames: 91008
